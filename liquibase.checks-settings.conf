# Liquibase Quality Checks Configuration
# This file intentionally has strict checks enabled to force failures

liquibase.command.checks.run.checksSettingsFile=liquibase.checks-settings.conf

# Enable a check that will fail
liquibase.command.checks.show.enabled=true
